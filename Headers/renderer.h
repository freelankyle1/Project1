#pragma once
#include "../pch.h"

/*
class Renderer
{
public:

	Renderer(ID3D11DeviceContext*);
	~Renderer();

	void renderStuff();

	static void beginScene(ID3D11DeviceContext*);

private:

	ID3D11DeviceContext* m_devcon;


};

*/